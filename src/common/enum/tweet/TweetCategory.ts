/**
 * Contain tweet category value
 */
export enum TweetCategory {

    /**
     * Unofficial tweet, (create by a lambda user)
     */
    UNOFFICIAL,

    /**
     * Official tweet, (create by an official user like TBM)
     */
    OFFICIAL
}
